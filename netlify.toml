[build]
  functions = "netlify/functions"
  publish = "."

[dev]
  functions = "netlify/functions"
  
# Environment variables for local development
# Note: For production, set these in Netlify dashboard under Site settings -> Environment variables
